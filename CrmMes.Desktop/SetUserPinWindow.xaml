<Window x:Class="CrmMes.Desktop.SetUserPinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PIN terminale" Icon="Assets/logo.ico" Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="P I N   T E R M I N A L E" Style="{StaticResource MicroLabel}" Foreground="{StaticResource PrimaryBrush}" />
            <TextBlock x:Name="UserNameText" Style="{StaticResource EditorialTitle}" FontSize="24" Margin="0,0,0,4" />
            <TextBlock Text="Il PIN identifica questo utente al Terminale di reparto (avvio/completamento fasi, fermi, non conformità). Da 4 a 8 cifre." Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,18,0,0">
            <TextBlock Text="Nuovo PIN" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4" />
            <PasswordBox x:Name="PinBox" FontSize="18" Height="40" VerticalContentAlignment="Center" MaxLength="8" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,14,0,0">
            <TextBlock Text="Conferma PIN" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4" />
            <PasswordBox x:Name="ConfirmPinBox" FontSize="18" Height="40" VerticalContentAlignment="Center" MaxLength="8" />
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,10,0,0" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Salva PIN" Width="110" Height="36" Margin="0,0,10,0" Click="Save_Click" />
            <Button Style="{StaticResource SecondaryButton}" Content="Annulla" Width="100" Height="36" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
