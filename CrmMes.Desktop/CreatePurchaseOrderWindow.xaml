<Window x:Class="CrmMes.Desktop.CreatePurchaseOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nuovo ordine fornitore" Icon="Assets/logo.ico" Height="560" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nuovo ordine fornitore" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,18" />

        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBlock Text="Fornitore" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5" Margin="0,0,0,5" />
            <ComboBox x:Name="SupplierCombo" DisplayMemberPath="Name" Height="34" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ItemCodeBox" Grid.Column="0" Margin="0,0,10,0" Tag="Codice materiale" />
            <TextBox x:Name="ItemQuantityBox" Grid.Column="1" Margin="0,0,10,0" Text="1" />
            <TextBox x:Name="ItemPriceBox" Grid.Column="2" Margin="0,0,10,0" Text="0" />
            <Button Grid.Column="3" Content="Aggiungi riga" Click="AddItem_Click" />
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource CardBorder}" Padding="4" MinHeight="140">
            <ListView x:Name="ItemsList">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Codice materiale" DisplayMemberBinding="{Binding MaterialCode}" Width="200" />
                        <GridViewColumn Header="Quantità" DisplayMemberBinding="{Binding Quantity}" Width="90" />
                        <GridViewColumn Header="Prezzo unitario" DisplayMemberBinding="{Binding UnitPrice}" Width="110" />
                        <GridViewColumn Header="" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Rimuovi" Style="{StaticResource SecondaryButton}" Height="28" Click="RemoveItem_Click" Tag="{Binding}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <TextBlock Grid.Row="4" x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,10,0,0" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Style="{StaticResource SecondaryButton}" Content="Annulla" Width="100" Height="36" Margin="0,0,10,0" Click="Cancel_Click" />
            <Button x:Name="CreateButton" Content="Crea ordine" Width="140" Height="36" Click="Create_Click" />
        </StackPanel>
    </Grid>
</Window>
