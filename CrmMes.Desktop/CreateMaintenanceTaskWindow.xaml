<Window x:Class="CrmMes.Desktop.CreateMaintenanceTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nuovo intervento di manutenzione" Icon="Assets/logo.ico" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <StackPanel Margin="24">
        <TextBlock Text="Nuovo intervento di manutenzione" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,18" />

        <TextBlock Text="Macchina" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5" Margin="0,0,0,5" />
        <ComboBox x:Name="EquipmentCombo" DisplayMemberPath="Name" Height="34" Margin="0,0,0,14" />

        <TextBlock Text="Tipo" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5" Margin="0,0,0,5" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
            <RadioButton x:Name="PreventiveRadio" Content="Preventiva" GroupName="Type" IsChecked="True" Checked="Type_Changed" Margin="0,0,20,0" VerticalAlignment="Center" />
            <RadioButton x:Name="CorrectiveRadio" Content="Correttiva" GroupName="Type" Checked="Type_Changed" VerticalAlignment="Center" />
        </StackPanel>

        <TextBlock Text="Titolo" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5" Margin="0,0,0,5" />
        <TextBox x:Name="TitleBox" Margin="0,0,0,14" />

        <TextBlock Text="Descrizione (opzionale)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5" Margin="0,0,0,5" />
        <TextBox x:Name="DescriptionBox" Margin="0,0,0,14" />

        <StackPanel x:Name="PreventivePanel">
            <TextBlock Text="Scadenza (opzionale)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5" Margin="0,0,0,5" />
            <DatePicker x:Name="DueDatePicker" Margin="0,0,0,14" />

            <TextBlock Text="Ricorrenza in giorni (opzionale — completandolo, genera subito la prossima occorrenza a questa distanza)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5" Margin="0,0,0,5" TextWrapping="Wrap" />
            <TextBox x:Name="RecurrenceDaysBox" Margin="0,0,0,14" />
        </StackPanel>

        <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,0,0,10" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button Style="{StaticResource SecondaryButton}" Content="Annulla" Width="100" Height="36" Margin="0,0,10,0" Click="Cancel_Click" />
            <Button x:Name="CreateButton" Content="Crea intervento" Width="150" Height="36" Click="Create_Click" />
        </StackPanel>
    </StackPanel>
</Window>
