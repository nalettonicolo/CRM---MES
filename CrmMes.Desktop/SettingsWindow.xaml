<Window x:Class="CrmMes.Desktop.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Impostazioni" Height="300" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Indirizzo server" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
        <TextBlock Grid.Row="1" Text="Indirizzo dell'API a cui il client si collega. Cambialo per puntare a un server pubblicato invece che a quello locale." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,8,0,16" />

        <TextBlock Grid.Row="2" Text="URL API" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12.5" Margin="0,0,0,5" />
        <TextBox Grid.Row="3" x:Name="ApiUrlBox" Height="34" />

        <StackPanel Grid.Row="4" VerticalAlignment="Bottom">
            <TextBlock x:Name="TestResultText" Margin="0,14,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="TestButton" Style="{StaticResource SecondaryButton}" Content="Verifica connessione" Width="150" Height="36" Click="TestButton_Click" Margin="0,0,10,0" />
            <Button x:Name="SaveButton" Content="Salva e riavvia connessione" Width="190" Height="36" Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
