# Copy to .env (gitignored) and fill in real values before running docker compose.
NEON_DATABASE_URL=Host=...;Port=5432;Database=neondb;Username=...;Password=...;SSL Mode=Require;Channel Binding=Require
CRM_MES_JWT_KEY=replace-with-a-random-string-at-least-32-characters-long
